import { OddsFormatPipe } from './odds-format.pipe';

describe('OddsFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new OddsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
